// import { redirect } from '@sveltejs/kit';
// import handlesAddress from '$lib/handler';

// export const actions = {
   
//     criarEndereco: async ({ request, locals: { supabase } }) => {
//         const formData = await request.formData();
//         handlesAddress(formData);
        

        
//     }

   
// };



{/* <script> 
  let cep = '', rua = '', bairro = '', cidade = ''; 
  $: if (cep.length === 8) 
    fetch(`https://viacep.com.br/ws/${cep}/json/`) 
      .then(r => r.json()) 
      .then(d => { rua = d.logradouro; bairro = d.bairro; cidade = d.localidade }); 
</script> 
 
<input bind:value={cep} maxlength="8" placeholder="CEP" /> 
<input bind:value={rua} placeholder="Rua" /> 
<input bind:value={bairro} placeholder="Bairro" /> 
<input bind:value={cidade} placeholder="Cidade" />  */}